The conda_environment.yaml file can be used to create a replica of the conda env used for the study

To use the .yaml file:
  conda env create --name insert_name -f environment.yml

The conda_specs.txt file has a list of the packages and their versons installed in the env 
